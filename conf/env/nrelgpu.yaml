# @package _global_
defaults:
  - override /hydra/launcher: submitit_slurm
  - _self_

hydra:
  launcher:
    timeout_min: 1020
    cpus_per_task: 16
    mem_gb: 32
    gres: gpu
    account: extremedata
    partition: gpu-h100
